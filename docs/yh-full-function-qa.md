# 绘影工作室全功能可用性回归

更新时间：2026-06-16 15:27（Asia/Shanghai）

## 本轮范围

本轮按自动化 M7 执行入口级可用性回归，目标不是消耗真实模型额度，而是确认用户从首页、导航和直达路由能进入核心功能面，并记录阻塞点。参考目标仍是 ViMAX、Toonflow-app、ArcReel 的生产链路完整性，以及吉梦、可灵、海螺、Runway 这类商业产品的入口清晰度。

## 入口矩阵

| 入口 | 保留路径 | 验证方式 | 结果 | 证据 |
| --- | --- | --- | --- | --- |
| 首页 / 创作工作台 | `/` | 浏览器打开 `http://localhost:5001/` | 通过 | 命中“绘影工作室 / 镜头生成 / 剧本分镜 / 流程画布” |
| AI 视频 | 左侧“视频” | 点击 `AI 视频创作` | 通过 | 命中“视频创作 / 专业视频 / 提示词” |
| 图片生成 | 首页“角色与场景”行动卡 | 点击 `button[title="角色与场景"]` | 通过 | 命中“图片生成 / 模型 / 提示词 / 生成结果 / 图片生成助手” |
| 绘影精灵 / 剧本分镜 | 左侧“精灵” | 点击 `button[title="绘影精灵"]` | 通过 | 命中“绘影精灵 / 剧本” |
| 素材 / 图文资产 | 左侧“素材” | 点击 `button[title="图文与素材"]` | 通过 | 命中“图文与素材 / 素材” |
| 影视创作 | 左侧“影视” | 点击 `影视创作` | 通过 | 命中“影视创作 / 角色 / 场景 / 道具 / 分镜” |
| 任务中心 | 左侧“任务” | 点击 `任务中心` | 通过 | 命中“任务中心 / 任务” |
| 工作流画布 | 左侧“画布”或 `/node-editor` | 直达 `/node-editor` | 通过 | 命中“画布 / 节点 / 执行” |
| 平台研究 | 左侧“研究”或 `/research` | 直达 `/research` | 通过 | 命中“平台研究 / 研究” |
| 设置 / BYOK | 左侧“设置”或顶部“API 设置” | 点击 `设置与 BYOK` | 通过 | 命中“API Base / API Key / BYOK” |

## 本轮发现

1. 图片入口原本能工作，但首页行动卡缺少稳定 `aria-label/title`，自动化和无障碍验证只能通过重复文本模糊命中，容易点错到画廊卡片。
2. 左侧“素材 / 精灵”等导航已经有 `title`，可稳定进入；但不同入口的可访问名称策略仍不统一，后续应统一成显式 `aria-label`。
3. 当前没有执行真实模型生成；最长稳定生成能力本轮未测，仍停留在入口级和任务接口级验证。后续需要按低成本阶梯从文本、图片到单镜头视频逐级测。

## 后续 M7 验收口径

- 每次前端入口改动后，必须至少回归首页、视频、图片、精灵、影视、素材、任务、设置、画布、研究 10 个入口。
- 前端点击通过不代表生成链路通过；模型调用仍要按 M1/M2/M3 阶梯单独验证。
- 若入口文案继续调整，保留路径应以 `aria-label/title/data-testid` 为准，避免只依赖可见文案。

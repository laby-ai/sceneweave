# 绘影工作室发布入口审计

审计时间：2026-06-15

本页记录公网发布前的核心入口可达性检查。功能边界以 `docs/product-function-retention.md` 为准：视觉和信息架构可以优化，但不能删除或隐藏绘影已有能力。

## 浏览器点击矩阵

| 能力 | 入口 | 点击结果 | 证据 |
| --- | --- | --- | --- |
| 首页创作工作台 | `/` | 通过 | 首页可见 `绘影工作室` 与 `角色、分镜、图像、视频和资产管理集中在一个创作入口。` |
| AI 视频生成 | 左侧导航 `创作` | 通过 | 点击后可见标题 `AI视频`。 |
| 图片生成 | 首页卡片 `图像创作` | 通过 | 点击后可见 `AI 图像创作`、`图像创作助手`。 |
| 绘影精灵/剧本分镜 | 左侧导航 `精灵` | 通过 | 点击后可见标题 `绘影精灵`。 |
| 图文/素材创作 | 首页按钮 `图文创作` | 通过 | 点击后可见 `选择创作类型`、`图片生成`、`海报生成`、`文案生成`、`小红书推送`、`公众号推送`。 |
| 影视创作 | 左侧导航 `故事板` | 通过 | 点击后可见 `影视创作`、`未命名剧本`。 |
| 任务中心 | 左侧导航 `任务` | 通过 | 点击后可见标题 `任务中心`。 |
| 工作流画布 | 左侧导航 `画布` | 通过 | 点击后进入 `/node-editor`，可见 `节点式视频编辑器`、`开始创作`。 |
| 平台研究 | 首页按钮 `研究` | 通过 | 点击后进入 `/research`，可见 `AIGC 长视频平台研究`、`平台优先级排名`。 |
| 设置 / BYOK | 左侧导航 `设置` | 通过 | 点击后可见 `API Base`、`API Key`、`BYOK`。 |

## 本轮发现与修复

- 问题：`任务` 在顶部栏、Hero 操作区和左侧导航中同时出现，浏览器可访问名称容易被解析成多个同名按钮。
- 修复：顶部任务按钮增加 `aria-label="查看任务中心，当前 N 个任务"`，Hero 任务按钮增加 `aria-label="打开任务中心"`，左侧导航 `任务` 保持唯一入口名。
- 复测：`getByRole('button', { name: '任务', exact: true })` 的可见匹配数从 2 降为 1，点击后可见 `任务中心`。

## 仍需后续覆盖

- 深层生成表单的真实提交链路仍需单独验收，包括视频、图片、影视创作和工作流画布导出后的 API 调用。
- BYOK 只验证了入口可见，尚未在本轮输入测试 API Base / API Key 或调用 `/api/provider/test`。
- `/research` 是研究页面，发布时建议继续确认其中平台对比内容是否为最新资料。

// Vendored infinite-canvas (basketikun/infinite-canvas) is compiled by the bundler
// via the @icanvas alias. We expose it to TypeScript as `any` so the large vendored
// tree does not pollute our strict type-check gate.
declare module '@icanvas/*';
